/**
 * Session management utilities.
 * Currently using stateless JWTs handled in jwt.ts and trpc context.
 */

export const getSession = async () => {
  // Placeholder for future session-based auth if needed
  return null;
};
